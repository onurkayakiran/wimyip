# Proje Planı — IP / ASN / Domain Arşivi

Bu dosya projenin fazlarını ve ilerleme durumunu takip eder. Tam mimari
detayları için konuşma geçmişindeki plana bakılabilir; burada sadece
faz/görev checklist'i tutulur. Her madde tamamlandıkça işaretlenir.

## Faz 1 — İskelet
- [x] docker-compose.yml (mongodb, redis, backend, worker, beat, frontend)
- [x] .env.example / .gitignore
- [x] Backend iskeleti (FastAPI + health endpoint)
- [x] Frontend iskeleti (React + Vite + nginx)
- [x] Taşınabilirlik: scripts/backup.sh, scripts/restore.sh

## Faz 2 — RIR Ingestion
- [x] RIR delegated-extended parser (IPv4 + ASN)
- [x] Mongo upsert (prefixes, asns koleksiyonları + index'ler)
- [x] Celery task + günlük beat zamanlaması
- [x] API: /api/prefixes, /api/asns, /api/lookup/ip/{ip}, /api/stats
- [x] Frontend: dashboard sayacı + IP arama

## Faz 3 — RDAP Whois + Sahiplik Geçmişi
- [x] RDAP sorgu istemcisi (her RIR'in RDAP servisine dogrudan sorgu, org bilgisi cikarma)
- [x] whois_snapshots koleksiyonu (ham RDAP JSON arşivi)
- [x] asn_org_history + prefix_org_history diff mantığı (sahiplik değişim arşivi)
- [x] Hız-sınırlı, devam edilebilir (cursor tabanlı) arka plan zenginleştirme görevi (beat: her 5 dakikada bir)
- [x] API: GET /api/asns/{asn}/history, POST /api/asns/{asn}/refresh, GET /api/prefixes/{cidr}/history, POST /api/prefixes/{cidr}/refresh

## Faz 4 — BGP (RIPEstat) + PeeringDB
- [x] RIPEstat announced-prefixes entegrasyonu (ASN başına geçmiş+güncel duyurulan prefixler, zaman aralıklarıyla)
- [x] prefix_asn_history koleksiyonu (güncel + geçmiş ASN↔prefix eşlemesi, IP lookup'a entegre)
- [x] PeeringDB org zenginleştirme (net + org detayı, asn_peeringdb_info)
- [x] Ortak resumable batch runner (batch_runner.py) — RDAP/BGP/PeeringDB tarafından paylaşılıyor
- [x] API: GET /api/asns/{asn}/prefixes, /peeringdb, POST .../refresh-bgp, .../refresh-peeringdb; /api/lookup/ip/{ip} artık BGP bilgisini de içeriyor

## Faz 5 — Domain Keşfi
- [x] Certificate Transparency log kesfi — crt.sh'nin herkese acik Postgres DB'si uzerinden (`ct_log_entry` cursor + ham sertifikayi kendimiz parse ediyoruz, cunku `certificate_identity` toplu taramaya kapatilmis). **Not:** Plandaki "ct-listener" websocket/streaming servisi yerine Celery beat uzerinde periyodik (2 dakikada bir) resumable poller olarak kuruldu — guvenilir ucretsiz bir gercek-zamanli CT stream servisi kalmadigi icin bu, aym sonucu (surekli buyuyen domain arsivi) daha saglam bir mekanizmayla veriyor.
- [x] PTR sweep worker — kendi Unbound resolver container'imiz (`unbound`) uzerinden, ayri Celery kuyrugunda (`ptr-worker`), hiz-sinirli ve checkpoint'li (sadece allocated RIR bloklarini tarar)
- [x] domains koleksiyonu (hem ct_log hem ptr kaynaklarini `sources` alaninda birlikte tutuyor)
- [x] API: GET /api/domains, /api/domains/{domain}; /api/lookup/ip/{ip} artik PTR kaydini da iceriyor

## Faz 6 — DNS Geçmiş Takibi
- [x] dns_history_sync periyodik görevi (bilinen domainler için A/AAAA/NS yeniden çözümleme, kendi Unbound resolver'imiz uzerinden, resumable cursor ile)
- [x] domain_ip_history / domain_ns_history güncelleme mantığı (her gozlemlenen deger kendi first_seen/last_seen'iyle ayri satir, gecmis hic ezilmiyor)
- [x] nameserver_ip_history — her NS hostname'inin KENDI A/AAAA kaydinin ayri gecmisi
- [x] API: GET/POST /api/domains/{domain}/history|refresh-dns, GET /api/nameservers/{ns}/history|domains
- [x] /api/lookup/ip/{ip} artik "bu IP bir nameserver ise hangi domainlere hizmet veriyor" bilgisini de iceriyor (tam cember: RIR + BGP + PTR + nameserver-domain baglantisi tek sorguda)

## Faz 7 — Frontend Genişletme
- [x] React Router ile çok sayfalı yapı (Home, IP, Prefix, ASN, Domain, Nameserver, Search)
- [x] Prefix detay sayfası (RIR + RDAP sahiplik geçmişi; BGP'de gorulen ama RIR'da tam eslesmeyen alt-prefixler icin otomatik en-dar-kapsayan-blok fallback'i)
- [x] ASN detay sayfası (RIR + RDAP gecmisi + PeeringDB profili + BGP duyurulan prefixler, hepsi "Şimdi Sorgula" ile anlik tetiklenebilir)
- [x] Domain detay sayfası (IP + NS geçmişi zaman çizelgesi, PTR baglantilari, "DNS Şimdi Sorgula")
- [x] Nameserver detay sayfası (kendi IP geçmişi + hizmet verdiği domainler)
- [x] Birleşik arama (IP/CIDR/ASN/domain istemci tarafinda pattern-tespitiyle doğrudan yönlendirme; org adı için backend /api/search)
- [x] Dashboard'a arka plan görev durumu (ingestion_jobs) paneli eklendi
- [x] Backend API: GET /api/search, GET /api/status
- [x] Gerçek tarayıcıda (Playwright) uçtan uca doğrulandı: dashboard → ASN → prefix (BGP alt-prefix fallback dahil) → domain → nameserver → IP, konsol hatasız. Bu sırada iki gerçek veri tutarlılığı hatası bulunup düzeltildi: (1) BGP'nin gördüğü spesifik prefixler RIR tablosunda tam eşleşmiyordu, (2) refresh-dns ile manuel sorgulanan domainler `domains` ana koleksiyonuna hiç yazılmıyordu.
- [x] Arka plan görevleri için dashboard'da canlı sağlık göstergesi (🟢/🔴, 20 sn'de bir yenilenir); `unbound`'u bilerek durdurup PTR taramasının %90+ hata oranını yakaladığını doğruladım (önceden bu durum sessizce "ok" görünüyordu)
- [x] IP sayfasına "BGP Verisini Şimdi Topla" butonu — RIPEstat'ın prefix-overview'ı ile o an duyuran ASN'i canlı bulup tam geçmişini tarıyor (daha önce hiç BGP verisi olmayan bir IP için bile çalışır, çünkü hangi ASN'in sorgulanacağını kendisi keşfediyor)
- [x] crt.sh'nin replica veritabanindan gelen gecici "recovery conflict" hatalari icin otomatik yeniden deneme (taze baglantiyla, 3 deneme)
- [x] **Altyapi duzeltmesi:** nginx `proxy_pass`'i backend'in Docker IP'sini kalici onbelleklemesi, backend her yeniden olusturuldugunda (rebuild/restart) frontend'de 502 hatasina yol aciyordu. `resolver 127.0.0.11 valid=10s` + degiskenli proxy_pass ile backend her yeniden basladiginda frontend'e dokunmadan otomatik toparlanacak sekilde duzeltildi ve gercek senaryoyla (backend'i yeniden olusturup) dogrulandi.
- [x] **Veri bütünlüğü düzeltmesi:** `worker`/`beat`/`ptr-worker` container'ları `backend`'e `depends_on` ile bağlı olmadığı için, backend henüz index'lerini kurmadan bu süreçler yazmaya başlayabiliyordu — bu yarış durumu `nameserver_ip_history`'de gerçek yinelenen kayıtlara ve birkaç koleksiyonda index'in hiç kurulamamasına yol açmıştı (restore sırasında fark edildi). Index tanımları `index_defs.py`'da tek yerde toplandı, her index ayrı try/except ile korunacak şekilde `indexes.py`'a taşındı, ve artık her Celery süreci (worker/beat/ptr-worker) kendi başlangıcında (backend'den bağımsız) kendi index'lerini garanti ediyor. Var olan yinelenen kayıtlar temizlendi, tam yedek/geri-yükle round-trip'i hatasız doğrulandı.
- [x] **Yedekleme:** `backup` servisi (docker-compose) `docker compose up -d` ile otomatik başlıyor, varsayılan 24 saatte bir `mongodump` alıp son 7 yedeği tutuyor (named volume'de, macOS'un Desktop-klasörü izin kısıtına takılmamak için host bind-mount kullanmıyor). `scripts/export_backups.sh` (docker cp ile host'a çıkarır), `scripts/backup.sh` (anlık, doğrudan host'a), `scripts/restore.sh`/`restore_latest.sh` (geri yükleme) — hepsi gerçek veriyle (261k+ prefix, 427k+ toplam doküman) uçtan uca test edildi.
- [x] `scripts/restart.sh` — servisleri elle (tekil/coklu/hepsi, gerekirse `--build` ile) yeniden baslatma kisayolu.
- [x] **Pymongo fork-safety düzeltmesi:** `ensure_indexes_sync()`'in Celery modülü yüklenirken (fork'tan ÖNCE, ana süreçte) çağrılması "MongoClient opened before fork" riskine yol açıyordu (forklanan alt süreçler ana sürecin bağlantısını miras alır, kilitlenme riski). `worker_process_init` sinyaline taşındı (her forklanan alt süreç kendi bağlantısını fork SONRASI kurar). Bu değişiklik yeni bir yarış durumu ortaya çıkardı (birden fazla alt süreç aynı index'i aynı anda kurmaya çalışıp çakışıyordu) — `indexes.py` artık "önce dene, çakışırsa index zaten var mı kontrol et, yoksa backoff ile tekrar dene" mantığıyla bu yarışa dayanıklı. `worker` servisinin concurrency'si de (varsayılan host CPU sayısı, örn. 14) 2'ye sabitlendi.
- [x] **Güvenlik iyileştirmesi:** backend/worker/beat/ptr-worker artık root yerine ayrıcalıksız `appuser` (uid 1000) olarak çalışıyor. **Ek düzeltme:** appuser'a geçiş `/app`'in sahipliğini değiştirmediği için `beat` kendi zamanlama dosyasını (`celerybeat-schedule`) yazamayıp sürekli çöküyordu (hiçbir periyodik görev tetiklenmiyordu) — Dockerfile'da `chown -R appuser:appuser /app` ile düzeltildi.
- [x] **Worker concurrency düzeltmesi:** aynı kuyrukta 6 farklı periyodik görev (rdap-asn, rdap-prefix, bgp, peeringdb, ct-log, dns-history) olduğu için concurrency=2 yetersiz kaldı — sık çalışan (2 dk) ct-log görevi sürekli slot kapatıp 5 dk'lık diğer görevlerin saatlerce aç kalmasına yol açtı. 6'ya çıkarıldı (index çakışması ayrıca çözüldüğü için artık yüksek concurrency güvenli).
- [x] **Fiziksel sunucu uyumluluğu:** mongo:5.0+ CPU'da AVX desteği zorunlu kılar, AVX'i olmayan eski sunucularda `mongod` "Illegal instruction" ile çöküyor. `MONGO_IMAGE_TAG` ortam değişkeniyle yapılandırılabilir hale getirildi (varsayılan `7`, AVX'siz makinelerde `.env`'de `4.4` yapılır — bu projede 5.0+'a özgü hiçbir özellik kullanılmadığı için tam uyumlu). Healthcheck hem `mongosh` (7+) hem eski `mongo` (4.4) kabuğuyla çalışacak şekilde güncellendi. `backup` servisinin image'ı da aynı etikete göre build ediliyor (build arg).
- [x] **CT log senkronu self-healing:** cursor, crt.sh'nin (birden fazla replica'dan yanıt veren) güncel maksimumundan 1M+ ID geride kaldığı halde sürekli 0 sonuç dönerse (gözlemlenen gerçek senaryo: taşınan bir ortamda cursor kalıcı olarak sıkışmıştı), artık otomatik olarak "şimdi"ye yakın bir noktaya atlıyor — bir daha sessizce sonsuza kadar takılı kalamaz.
- [x] **Sertifika ayrıştırma dayanıklılığı:** eski/standart-dışı bir sertifikanın bozuk ASN.1 alanı `cryptography` kütüphanesinin katı ayrıştırıcısını kırıp TÜM batch'i (500 sertifikanın 499'u dahil) düşürüyordu — `_extract_domains` artık tüm ayrıştırma+extension erişimini tek try/except ile sarıyor, tek bozuk sertifika sessizce atlanıyor.
- [x] **DNS geçmişine MX/TXT eklendi:** `resolve_mx_records`/`resolve_txt_records` (dns_client.py), `sync_domain_dns` artık `domain_mx_history`/`domain_txt_history` koleksiyonlarını da aynı first_seen/last_seen pattern'iyle dolduruyor (TXT için uzun değerlerde index boyutu sınırına takılmamak adına `value_hash` üzerinden unique index). `GET /api/domains/{domain}/history` ve Domain detay sayfası (MX/TXT bölümleri, TXT hover'da tam metin) buna göre genişletildi. Mevcut `refresh-dns` butonu ve 5 dk'lık beat taraması otomatik kapsıyor, ek endpoint/görev gerekmedi.
- [x] **MongoDB artık harici (external):** Sunucudaki bir mongo:7→mongo:4.4 WiredTiger format uyumsuzluğu (fatal assertion, crash-loop) sonrası kullanıcı MongoDB'yi kendi yöneteceğine karar verdi. `docker-compose.yml`'den `mongodb` servisi ve `mongo_data` volume'u tamamen kaldırıldı; `backend`/`worker`/`beat`/`ptr-worker` artık `.env`'deki tek bir `MONGO_URI`'yi `env_file` üzerinden olduğu gibi kullanıyor (önceden `MONGO_ROOT_USER`/`PASSWORD`'den satır satır inşa ediliyordu). `backup` servisi de aynı `MONGO_URI` ile harici sunucuya bağlanıyor; `scripts/backup.sh`/`restore.sh` artık `docker compose exec mongodb` yerine `docker compose exec backup` kullanıyor. `MONGO_IMAGE_TAG` artık yalnızca backup'ın mongodump/mongorestore sürüm uyumluluğu için anlamlı. **Not:** Kullanıcının gerçek `.env`'i henüz güncellenmedi (MONGO_URI eklenmesi, eski MONGO_ROOT_USER/PASSWORD/PORT'un temizlenmesi manuel yapılmalı) — bkz. sohbet geçmişi.
- [ ] **GÜVENLİK - takip gerekiyor:** `04bb3ec` commit'inde yanlışlıkla `.envt` adlı bir dosya (gerçek `MONGO_ROOT_PASSWORD` dahil) repoya eklenip origin/main'e push'lanmış. Dosya `.gitignore`'da değil. Şifrenin rotate edilmesi ve dosyanın repodan/geçmişten temizlenmesi gerekiyor.
- [x] **Ülke bazlı (Türkiye) PTR/domain taraması — yeni container:** `ptr_batch.py`'deki `_next_target_ip`/`run_ptr_sweep_batch` opsiyonel `country`/`job_name`/`batch_size`/`rate_limit_seconds` parametreleriyle genişletildi (varsayılanlar global davranışı birebir korur). Yeni `ingestion.ptr_sweep_country` Celery task'ı (`ptr-country` kuyruğu, `TARGET_COUNTRY` env'ine göre, varsayılan `TR`) `ptr_sweep_country:<ülke>` adıyla global sweep'ten (`ptr_sweep`) tamamen ayrı bir cursor tutuyor. Yeni `ptr-worker-country` container'ı `ptr-worker` ile birebir aynı desende eklendi. `asns.country` index'i ve `prefixes` üzerinde `(country,version,start_ip,end_ip)` bileşik index'i eklendi. Gerçek (sahte TR/US prefix'li throwaway mongo) ortamda uçtan uca doğrulandı: ülke taraması sadece TR bloğunda kaldı (US bloğuna hiç dokunmadı), global tarama ise değişmeden her iki bloğu da tarayıp eski `ptr_sweep` cursor'unu kullanmaya devam etti — iki iş birbirini hiç etkilemedi. Ayrıca kullanıcının isteğiyle, zaten var olan `GET /api/status` (ingestion_jobs, isim filtresi yok) kullanılarak `Home.jsx`'teki görev-durumu tablosu paylaşılan bir `JobStatusPanel` component'ine çıkarılıp hem ana sayfada hem `/admin` panelinde gösterilecek şekilde eklendi (backend değişikliği gerekmedi, yeni görev otomatik listede çıkıyor).
- [x] **Türkiye apex (ana) domain taraması — yeni container:** `domain_ip_history`'ye (sadece ileriye dönük) `ip_int` alanı ve index'i eklendi. Yeni `backend/app/ingestion/domain_apex_batch.py`: `tldextract` (sadece paketle gelen sabit Public Suffix List, ağ isteği yok) ile `.com.tr`/`.gov.tr` gibi çok parçalı Türk ccTLD'lerini doğru ayırt eden `_is_apex_domain()`; `run_domain_apex_batch()` `prefixes._id` üzerinden ilerleyen kendi cursor'unu (`domain_apex_scan:TR`) tutup, her Türkiye-tahsisli prefix için `domain_ip_history.ip_int` aralık sorgusuyla bilinen domainleri bulup SADECE apex olanlar için mevcut `sync_domain_dns()`'i çağırıyor ve `domains.sources`'a `tr_apex_scan` ekliyor (subdomain'ler hiç dokunulmadan olduğu gibi kalıyor). Yeni, tamamen ayrı `apex-worker-country` container'ı (kendi kuyruğu `apex-country`, `unbound`'a bağımlı değil çünkü IP taramıyor). Admin panelinde (`/admin`) "Türkiye Domain Taraması" butonuyla açılan yeni bir popup (`TrDomainsModal.jsx`, arama + pagination) — bunun için yeni bir backend endpoint'i YAZILMADI, zaten var olan `GET /api/domains?source=tr_apex_scan&q=&limit=&offset=` (`domains.py`) doğrudan kullanıldı. Gerçek container'larla (sahte TR prefix + apex/subdomain çifti) uçtan uca doğrulandı: sadece apex domain `sync_domain_dns` ile işlenip etiketlendi, subdomain hiç dokunulmadan kaldı, `/api/domains?source=tr_apex_scan` doğru filtreliyor, `/admin` sayfası sorunsuz build/serve ediyor.
- [x] **Bug düzeltmesi: `/api/status` domain_apex_scan:TR yüzünden 500 veriyordu:** `run_domain_apex_batch`, `ingestion_jobs.cursor`'a ham bir MongoDB `ObjectId` (prefix `_id`'si) yazıyordu — `GET /api/status` tüm `ingestion_jobs` dokümanlarını JSON'a çevirirken `ObjectId` serialize edilemediği için endpoint çöküyordu. Frontend'deki sessiz `.catch(() => {})` yüzünden bu, "Arka Plan Görevleri" panelinin sonsuza kadar "Yükleniyor..." göstermesi olarak ortaya çıktı (gerçek tarama görevleri bundan etkilenmiyordu, sadece bu görüntüleme endpoint'i kırıktı). Düzeltme: cursor artık `str(ObjectId)` olarak saklanıyor, okunurken tekrar `ObjectId(...)`'e çevrilip sorguda kullanılıyor. Gerçek container'larla doğrulandı: `/api/status` artık 200 dönüyor, ikinci bir tur string cursor'dan doğru şekilde devam edip normal şekilde başa sarıyor.
- [x] **Tek seferlik backfill: `domain_ip_history.ip_int`:** `apex-worker-country`'nin sadece ileriye dönük veriyle çalışması (bkz. bir önceki madde) 1 gün sonra çok az sonuç bulmasına yol açınca, kullanıcı beklemek yerine geriye dönük doldurmaya karar verdi. Yeni `backend/app/ingestion/backfill_ip_int.py` — `domain_ip_history`'yi (hedefleyen bir index olmadığı için kaçınılmaz olarak) tek seferlik tam tarayıp IPv4 kayıtlardan `ip_int`'i eksik olanları `bulk_write` ile (5000'lik gruplar) dolduruyor, IPv6 ve zaten dolu olanları atlıyor. `docker compose exec backend python -m app.ingestion.backfill_ip_int` ile elle çalıştırılır, beat zamanlamasına eklenmedi. Karışık test verisiyle (ip_int'siz IPv4, zaten dolu IPv4, IPv6) doğrulandı: sadece eksik olan doğru dolduruldu, diğer ikisi dokunulmadan kaldı.
- [x] **BGP Peering (komşuluk) takibi eklendi:** Daha önce sadece "ASN hangi prefixleri duyuruyor" (prefix-origin) ve PeeringDB'nin kendi beyan ettiği profil bilgisi takip ediliyordu, gerçek BGP komşuluğu (peering) hiç yoktu. RIPEstat'in `asn-neighbours` endpoint'i (RIS route collector'larının BGP tablolarından gözlemlediği komşuluklar - `left`/`right`/`uncertain` yön + `power`/`v4_peers`/`v6_peers`) eklendi: `ripestat_client.fetch_asn_neighbours`, `bgp_sync.sync_asn_peering` (yeni `asn_peering_history` koleksiyonu, `{asn,neighbour_asn,direction}` unique index + `neighbour_asn` index). Mevcut `bgp-enrich` döngüsüne (`bgp_batch.py`) tek satır eklenerek entegre edildi — yeni bir task/kuyruk/beat girdisi gerekmedi, mevcut prefix-duyuru senkronizasyonu hiç değişmeden yanına eklendi. `POST /asns/{asn}/refresh-bgp` artık hem prefixleri hem peering'i birlikte tazeliyor, yeni `GET /asns/{asn}/peers` endpoint'i eklendi. ASN detay sayfasına "BGP Komşuları (Peering)" bölümü eklendi. Gerçek Google (AS15169) verisiyle uçtan uca doğrulandı: RIPEstat'ten gerçek 333 komşu çekilip yazıldı (RIPEstat'in kendi `neighbour_counts.unique` değeriyle birebir eşleşti), API doğru sıralı/sayfalı döndü, sayfa sorunsuz serve etti.
- [x] **Anasayfada ziyaretçinin IP adresi:** Yeni `GET /api/whoami` (DB'ye dokunmaz) `X-Real-IP` header'ını (nginx zaten `frontend/nginx.conf`'ta `$remote_addr` ile set ediyordu) okuyup ziyaretçinin gerçek IP'sini döndürüyor. Anasayfada istatistik sayaçlarının üstüne, tıklanınca `/ip/{ip}`'ye giden belirgin bir "IP Adresiniz" banner'ı eklendi. Gerçek tarayıcıda (claude-in-chrome) doğrulandı: nginx üzerinden doğru IP gösterildi, sahte bir `X-Real-IP` header'ı göndermeyi denedim nginx bunu kendi gerçek değeriyle ezip spoofing'i engelledi, banner'a tıklayınca doğru IP detay sayfasına yönlendi.
- [x] **Türkiye Domain popup'ı yavaşlığı + arama UX'i düzeltildi:** `domains` koleksiyonunda (16M+ doküman) `sources` alanında index olmaması, admin panelindeki `source=tr_apex_scan` filtresinin her seferinde tam koleksiyon taraması yapmasına yol açıyordu (kullanıcının `htop` çıktısında birden fazla `mongod` iş parçacığının aynı anda %40-55 CPU'da çalıştığı gözlemlendi — birden fazla üst üste binen yavaş sorgunun kanıtı). `("domains", "sources", {})` index'i eklendi, `explain()` ile COLLSCAN'den IXSCAN'e geçtiği doğrulandı. Ayrıca `TrDomainsModal.jsx`'teki "her tuş vuruşunda anında ara" deseni, site genelindeki `SearchBar.jsx` ile tutarlı "yaz + Enter/Ara butonuna bas" (form submit) desenine çevrildi — hem index eksikliğinin sebep olduğu üst üste binen istekleri ortadan kaldırdı hem kullanıcının istediği net UX'i sağladı. Tarayıcıda (claude-in-chrome) doğrulandı: yazarken tablo değişmiyor, Enter'a basınca ve "Ara" butonuna tıklayınca ikisi de doğru şekilde arıyor.
- [x] **Cloudflare arkasında gerçek IP tespiti düzeltildi:** Production zinciri Cloudflare→sunucu nginx'i→docker `frontend` container'ı olduğu için, container'ın nginx'i `X-Real-IP`'yi kendi `$remote_addr`'ıyla (bir önceki hop'un/sunucu nginx'inin adresi, docker bridge gateway'i olarak görünüyordu — `172.20.0.1`) eziyordu. `frontend/nginx.conf`'ta bu satır kaldırılıp `X-Forwarded-For $proxy_add_x_forwarded_for` ile zincirleme (ezmeden ekleme) yapıldı. `backend`'in `/api/whoami`'si artık önce `CF-Connecting-IP`'yi (Cloudflare'in en güvenilir sinyali), sonra `X-Forwarded-For`'un ilk değerini, sonra `X-Real-IP`'yi, en son doğrudan bağlantıyı deniyor. 5 farklı header kombinasyonuyla test edilip hepsinin doğru IP'yi seçtiği doğrulandı.
- [x] **backup-loop.sh restart'ta gereksiz backup almasin:** Script `while true` donguisunde once backup alip SONRA sleep ediyordu - container her basladiginda (restart/deploy) script en bastan calisip her seferinde hemen yeni bir backup aliyordu, `BACKUP_INTERVAL_SECONDS`'a bakmadan. Donguye girmeden once en guncel yedek dosyasinin mtime'ina bakilip, INTERVAL henuz dolmamissa kalan sure kadar sleep edilip oyle girilecek sekilde duzeltildi (yedek yoksa veya INTERVAL zaten dolmussa hemen alinir). Uc senaryo da (yeni yedek/eski yedek/hic yedek yok) throwaway container'larla dogrulandi.
- [x] **Ayni sunucuda ama ayri compose projesinde calisan mongo'ya erisim (host-gateway):** `mongo-external/` (harici mongo:4.4) ile `asn` ayni fiziksel sunucuda ama iki farkli docker-compose projesi oldugu icin `MONGO_URI`'de `localhost` yazmak container'in KENDI loopback'ini isaret ediyordu ("connection refused"). `backend`/`worker`/`beat`/`ptr-worker`/`backup` servislerine `extra_hosts: ["host.docker.internal:host-gateway"]` eklendi (Docker 20.10+ Linux'ta da destekliyor), `MONGO_URI`'de host `host.docker.internal` olarak ayarlanmali. Ayrica `mongo-external`'in portu yanlislikla `127.0.0.1:27017` olarak baglanmisti (bu, hem ayni sunucudaki diger container'lardan hem internetten erisimi TAMAMEN engelliyordu, firewall'dan bagimsiz) - varsayilan (tum arayuzler) `27017`'ye duzeltildi, erisim kontrolu firewall'a (ufw/firewalld, sadece belirli kaynak IP'ye izin) birakildi. Lokal'de throwaway bir mongo-external instance'i ile uctan uca dogrulandi: backend `/api/health` `{"status":"ok"}` dondu, `backup` servisi uzerinden gercek mongodump/mongorestore (index'ler dahil) basariyla calisti.
- [x] **Yönetim paneli (/admin):** Servis durumu/logları görme ve servisleri yeniden başlatma için parola korumalı yeni bir sayfa eklendi. Docker socket erişimi güvenlik nedeniyle backend'e DEĞİL, izole yeni bir `control` servisine verildi (host'a geniş yetki riski sadece oraya sınırlı; dışarıya port açmıyor, sadece internal ağdan `CONTROL_SERVICE_TOKEN` ile erişilebilir). `control` kendi compose projesini container label'larından otomatik keşfediyor (`com.docker.compose.project`), bu yüzden proje adını ayrıca `.env`'de tanımlamaya gerek yok. Backend'deki `/api/admin/*` route'ları `X-Admin-Password` header'ını `ADMIN_PASSWORD`'e karşı doğrulayıp `control` servisine proxy'liyor (servis listesi + durumu, `docker logs` üzerinden canlı log — hata satırı sayımı dahil, ve restart). Frontend'de `/admin` sayfası parolayı `sessionStorage`'da tutuyor, 15 sn'de bir servis listesini yeniliyor. Gerçek ortamda (throwaway bir "harici" Mongo + tüm servisler ayağa kaldırılarak) uçtan uca doğrulandı: yanlış parola 401, doğru parola tam servis listesi (durmuş container'lar dahil), gerçek log çekme, ve `redis` servisinin fiilen restart edildiği log zaman damgasıyla kanıtlandı.
- [x] **Anasayfa istatistik sayaçları (504) düzeltmesi:** `GET /api/stats`, `prefixes`/`asns`/`domains` koleksiyonlarında her istekte canlı `count_documents({})` çalıştırıyordu; `domains` 16M+ doküman olduğu için bu tam tarama gerektiriyor, sonuç ~1 dk sürüyor ya da 504 ile hiç dönmüyordu. Mevcut Celery Beat periyodik görev deseni (`ingestion_jobs` + `/api/status`'un aynısı) kullanılarak: yeni `ingestion.refresh_stats_cache` görevi (`backend/app/ingestion/stats_cache_sync.py`, `STATS_REFRESH_INTERVAL_SECONDS`, varsayılan 300 sn) sync PyMongo ile üç koleksiyonda `estimated_document_count()` (milimetrik doğruluk yerine O(1) hızlı metadata okuma) hesaplayıp tek dokümanlı yeni `stats_cache` koleksiyonuna (`_id: "counts"`) yazıyor. `/api/stats` artık bu dokümanı `find_one` ile okuyor, cache boşsa (ilk deploy sonrası beat'in ilk turundan önceki kısa pencere) sıfır döndürüyor — canlı sayıma asla düşmüyor (aksi halde 504'ü yeniden yaratırdı). Frontend sözleşmesi (`{prefixes, asns, domains}`) değişmedi.
- [x] **Uzak sunucularda paralel worker — dar yetkili "mini API" mimarisi:** PTR/apex/RDAP gibi tarama işlerini farklı fiziksel sunucularda paralel çalıştırmak istendi, ama mevcut altyapı buna hazır değildi — `redis` (Celery broker) auth'suz, tek bir tam-yetkili `MONGO_URI` her worker'a aynen veriliyordu; uzak bir sunucuya bunları vermek, o sunucu ele geçirilirse tüm veritabanına/kuyruğa tam erişim demekti. Bunun yerine: uzak worker'ların Redis/Mongo'ya hiç dokunmadığı, sadece dar/amaca-özel bir HTTP API ile konuştuğu yeni bir izole servis eklendi — **`remote-api/`** (`control/` ile aynı felsefede ama tersine: bilhassa internete açık, `env_file` yerine sadece `MONGO_URI`/`MONGO_DB` + kendi ayarları açıkça verilen, `ADMIN_PASSWORD`/`CONTROL_SERVICE_TOKEN`/`CELERY_BROKER_URL`'e hiç erişimi olmayan minimal bir FastAPI servisi). İki uç nokta: `GET /v1/claim` (bir sonraki N tahsisli IPv4'ü `ingestion_jobs`'taki ayrı `ptr_sweep_remote` cursor'undan, unique-index tabanlı compare-and-swap ile atomik claim eder, `remote_batches`'a lease'li olarak yazar) ve `POST /v1/submit` (sadece o batch'te verilen tam IP kümesi için sonuç kabul eder, `ptr_records`/`domains`'e `sources: "remote:<etiket>"` etiketiyle yazar — kötü veri kaynağı token bazında geriye izlenebilir/temizlenebilir). Token'lar (`remote_worker_tokens`, sadece sha256 hash saklanır) `/admin` panelinden ("Uzak Workerlar" bölümü, `RemoteWorkersPanel.jsx`) üretilir/iptal edilir; heartbeat (`remote_worker_status`) aynı panelde canlı gösterilir. Uzak sunucuya deploy edilen **`remote-worker/`** (ayrı klasör, ayrı docker-compose) hiçbir inbound port açmaz, kendi sistem/ISP DNS resolver'ıyla PTR sorgusu yapıp sonucu API'ye gönderir — o makinede `REMOTE_API_TOKEN` dışında hiçbir sır bulunmaz. Sadece PTR taraması (`ptr_sweep`) uçtan uca uygulandı; RDAP/BGP/apex gibi diğer kuyruklar aynı `claim()`/`apply()` şablonuyla (`remote-api/app/registry.py`'ye eklenerek) sonra genişletilebilir. Mevcut yerel Celery/Redis/`ptr_batch.py`/`ptr_sync.py`/`control` altyapısına hiç dokunulmadı (tamamen ek). `remote-api` portu (`REMOTE_API_PORT`, varsayılan 8001) host'ta yayınlanıyor — önüne TLS'li bir subdomain (host-nginx/Cloudflare, repo dışı) eklenmesi gerekiyor, bkz. `remote-worker/README.md`. Frontend `npm run build` ile doğrulandı; canlı sunucuda `remote-api` ayağa kaldırılıp gerçek bir `remote-worker` ile uçtan uca (token üretme → claim → PTR sorgusu → submit → admin panelde görünme → token iptali sonrası 401) doğrulama kullanıcı tarafından yapılmalı.
- [x] **Ek düzeltme — `REMOTE_API_PORT` çakışması:** Varsayılan `8001`, kullanıcının gerçek `.env`'inde `BACKEND_PORT` zaten `8001` olduğu için çakışıyordu — `.env.example` ve `docker-compose.yml`'deki varsayılan `8002`'ye çekildi.
- [x] **Ek görev — `dns_history` kuyruğu uzak worker'lara açıldı:** PTR'dan sonra ikinci referans uçtan uca uygulama. `remote-api/app/queues/dns_history.py`: claim, `dns_history_batch.py`'deki keyset (domain adına göre) cursor sayfalamasıyla aynı mantıkla `ingestion_jobs`'taki ayrı `dns_history_remote` cursor'unu ilerletir; apply, `dns_history_sync.py::sync_domain_dns`'deki yazma mantığının (domain_ip_history/domain_ns_history/nameserver_ip_history/domain_mx_history/domain_txt_history, hepsi first_seen/last_seen ile) bir kopyasını kullanır — tek fark, dışarıdan/güvenilmeyen bir worker'dan geldiği için her liste alanı (`ips`/`nameservers`/`mx`/`txt`) makul bir üst sınıra (`_MAX_*`, 20-50 arası) kırpılıyor. `remote-worker/worker.py`, `QUEUE` env değişkenine göre `ptr_sweep` veya `dns_history` işleyicisini seçecek şekilde genelleştirildi (DNS sorguları `dns_client.py`'deki `resolve_records`/`resolve_mx_records`/`resolve_txt_records` mantığının bağımsız bir kopyası, `unbound`'a bağımlı değil — merkezdeki gibi kendi sistem/ISP resolver'ını kullanıyor). `remote-api`/`ClaimResponse`/`SubmitRequest` modelleri kuyruk-tipine-özel sabit şemalardan genel `list[dict]`'e çevrildi (DNS history'nin iç içe/zengin sonuç şekli için). Throwaway Mongo + gerçek Docker imajıyla uçtan uca doğrulandı: claim doğru domain sayfası döndü, submit'te batch dışı bir domain sessizce reddedildi, tüm 5 koleksiyona (`domains.sources` dahil `remote:<etiket>` etiketiyle) doğru yazıldı, 30 TXT kaydı gönderilince tam 20'de kırpıldığı doğrulandı. Bu sırada gerçek bir hata da bulunup düzeltildi: Motor/PyMongo'nun BSON datetime'ları naive (tzinfo'suz) döndürmesi yüzünden `lease_expires_at` karşılaştırması timezone-aware `now()` ile çakışıp 500 veriyordu (`stats.py`'deki mevcut düzeltmeyle aynı desenle giderildi, hem `ptr_sweep.py` hem `dns_history.py`'de).
- [x] **Ek görev — `dns_history_apex` kuyruğu (sadece ana/apex domainler):** CT-log senkronunun (`ct_sync.py`) wildcard sertifikalardan (örn. `*.cart.wb.com`) tek seferde binlerce subdomain eklemesi, `dns_history` kuyruğunun bunların hepsini sırayla işlemesine yol açıyordu — kullanıcı sadece ana domainlerin (subdomain değil) kayıtlarını önceliklendirmek istedi. Yeni `remote-api/app/queues/dns_history_apex.py`: `backend/app/ingestion/domain_apex_batch.py::_is_apex_domain()`'in (tamamen jenerik, `tldextract`'ın gömülü Public Suffix List'i ile `.com.tr`/`.co.uk` gibi çok parçalı suffix'leri de doğru ayırt eden) bir kopyasıyla `domains` koleksiyonunu (`domain` unique index'i sayesinde sıralı) 500'lük sayfalar halinde tarayıp sadece apex olanları biriktiriyor; yazma mantığı (`apply`) `dns_history.py`'den doğrudan import edilip tekrar kullanıldı (yazma tarafı iki kuyrukta birebir aynı). Yoğun subdomain bloklarında (wb.com örneği gibi) `claim()`'in süresiz uzamaması için `max_items * 50` ham domain'lik bir tarama üst sınırı var. **Test sırasında bulunup düzeltilen gerçek bir hata:** bu üst sınıra takılıp hiç apex domain bulunamadığında cursor hiç kaydedilmiyordu — bir sonraki `claim()` çağrısı aynı yoğun bloğu baştan tarayıp asla ilerleyemiyordu (600 sahte subdomain + 3 gerçek apex domainle uçtan uca test edilirken yakalandı). Düzeltme: cursor artık taranan her durumda (apex bulunsa da bulunmasa da) ilerletiliyor, sadece boş koleksiyon durumunda hiç yazılmıyor. Ayrıca `remote_worker_tokens.queues`'un zaten çoklu-kuyruk destekliyor olmasından yararlanılarak `remote-worker/docker-compose.yml` tek servisten üçe çıkarıldı (`worker-ptr`/`worker-dns`/`worker-dns-apex`, aynı imaj, sadece `QUEUE` farklı, `.env`'deki TEK `REMOTE_API_TOKEN`'ı paylaşıyorlar) — aynı sunucuda üç kuyruk aynı anda, tek token'la çalışabiliyor. Throwaway Mongo + gerçek Docker imajıyla uçtan uca doğrulandı: `.com`/`.co.uk`/`.com.tr` apex domainler doğru ayıklandı, subdomain'ler (`www.example.com`, `a.b.example.com`, `mail.ornekfirma.com.tr`) doğru atlandı, scan-limit sınırında cursor'un ilerlediği ve bir sonraki çağrının kaldığı yerden devam ettiği doğrulandı, submit `domains.sources`'a `remote:<etiket>` etiketiyle doğru yazdı.
- [x] **Admin panelde IP subnet listeleme + SYN port taraması (`port_scan` kuyruğu):** Kullanıcının verdiği tam scapy tabanlı SYN-tarama script'i (`ScannerConfig`/`ServiceDetector`/`PortScanner.scan_single_ip`) `remote-worker/port_scanner.py`'ye aynen taşındı (script'in kendi `SubnetScanner`/CLI katmanı atlandı — `scan_single_ip`'in zaten iç içe thread'lemesi olmadığı, `worker.py`'nin item-başı döngüsünün tek IP'yi zaten sıralı işlediği tespit edildi). Önceki 3 kuyruk (ptr/dns/dns-apex) **sürekli tarama** tipindeyken, bu **iş-tabanlı**: yeni `remote-api/app/queues/port_scan.py`, `find_one_and_update` ile bekleyen/lease'i dolmuş `port_scan_jobs` kaydını atomik claim edip subnet'i tüm IP'lere açıyor, `ClaimResponse`'a eklenen yeni `meta` alanıyla (diğer 3 kuyruk `None` döndürüyor, davranışları değişmedi) port_mode/custom_ports/delay_seconds worker'a taşınıyor. **Worker her IP'yi bitirir bitirmez sonucu HEMEN gönderiyor** (`worker.py`'de yeni `INCREMENTAL_QUEUES` dalı) — `apply()` her çağrıda `port_scan_jobs.scanned_count`/`current_ip`'i güncelleyip pending→claimed→running→completed geçişini yönetiyor (ayrı bir `$inc` sayacı yerine `count_documents` ile yeniden-sayım, worker'ın aynı sonucu tekrar göndermesi durumunda idempotent). SYN taramanın gerektirdiği ham soket erişimi (`CAP_NET_RAW`) SADECE yeni izole `Dockerfile.portscan`/`worker-portscan` container'ına veriliyor (`setcap cap_net_raw+eip` build-time'da root olarak sadece `python3` ikilisine, runtime'da `cap_drop: ALL` + `cap_add: NET_RAW`) — diğer 3 worker (`ptr`/`dns`/`dns-apex`) hâlâ hiçbir özel yetkiye sahip değil. Backend: `GET /api/prefixes` artık `q=` ile CIDR alt-dizesi VEYA doğrudan IP (içeren-prefix) araması destekliyor (public, veri zaten public); admin-auth korumalı yeni `POST/GET /admin/port-scans[...]` (host_count `PORTSCAN_MAX_HOSTS` — varsayılan 256 — aşılırsa 400, tekil IP `/32`'ye otomatik normalize edilir, `port_mode` varsayılanı kullanıcının istediği gibi "custom" — boş port listesi, asla popüler portlar değil). Frontend: yeni `PortScanPanel.jsx` (RemoteWorkersPanel deseninde inline panel — subnet arama/sayfalama + bağımsız "Hızlı Tara" girişi + canlı ilerleme çubuğu/`current_ip` gösteren iş listesi, aktif işler için 4 sn'de bir yenilenir) ve odaklı `PortScanTriggerModal.jsx`. Doğrulama gerçek Docker'da yapıldı: non-root (uid 1000) container'da scapy ile bilinen bir porta gerçek SYN atılıp SYN-ACK alındığı, `NET_RAW` olmadan aynı komutun kernel seviyesinde reddedildiği (negatif kontrol) kanıtlandı; `remote-api`'de claim/artımlı-submit/ilerleme/lease-expiry-kurtarma/idempotent-yeniden-gönderim uçtan uca test edildi; backend'in yeni endpoint'leri (auth, host_count sınırı, tekil IP, boş port listesi reddi) gerçek container'la doğrulandı; gerçek tarayıcıda (claude-in-chrome) tüm akış (arama → tetikleme → bekliyor/taranıyor/tamamlandı canlı geçişleri → sonuç detayı) görsel olarak doğrulandı — bu sırada ilerleme çubuğunun `scanned_count` henüz set edilmemişken "NaN%" gösterdiği gerçek bir hata bulunup düzeltildi.

## Faz 8 — GHCR + Argo CD CI/CD Zinciri (çekirdek servisler)
- [x] `k8s/` manifestleri: backend + 5 celery varyantı (worker/beat/ptr-worker/ptr-worker-country/apex-worker-country, hepsi aynı backend image'ı farklı `command` ile), frontend, remote-api (izole config/secret çifti korunarak), redis, unbound (Deployment+Service+PVC). `control` ve `backup` bu aşamada hariç (control docker.sock mount ediyor, backup basit cron-loop — ikisi de ayrı bir karar gerektiriyor).
- [x] Servis adı/port sözleşmesi kod ile birebir eşleştirildi: `backend` Service'i (nginx.conf'un beklediği), `redis`/`unbound` Service'leri (config.py varsayılanlarının beklediği) — hepsi ClusterIP; `frontend`/`remote-api` kullanıcı tercihiyle NodePort (30080/30082).
- [x] `.github/workflows/deploy.yml`: `paths-ignore: k8s/**` ile sonsuz döngü engellendi, `dorny/paths-filter` ile sadece değişen bileşen (backend/frontend/remote-api) build+GHCR push ediliyor, image tag'i sed ile ilgili tüm k8s dosyalarında güncellenip commit'leniyor.
- [x] `argocd/wimyip-app.yaml`: `k8s/` path'ini izleyen, otomatik prune+selfHeal Argo CD Application tanımı (kullanıcı tarafından elle `kubectl apply` edilecek).
- [x] Sır yönetimi: gerçek Mongo URI/parola, `ADMIN_PASSWORD`, `CONTROL_SERVICE_TOKEN` git'e hiç girmedi — `k8s-secrets.local.env` / `k8s-secrets-remote-api.local.env` (repo kökünde, `.gitignore`'a eklendi, `.env` ile aynı desen) kullanıcının paylaştığı gerçek değerlerle dolduruldu, `kubectl create secret --from-env-file` ile elle uygulanacak.
- [x] `DEPLOYMENT.md`: mimari kararlar, servis/port tablosu, sır yönetimi ve adım adım kurulum sırası (GHCR pull secret → Secret'lar → Argo CD repo bağlantısı → Application apply → ilk push) tek dosyada belgelendi.
- [ ] **Kullanıcı tarafında doğrulanacak** (bende cluster erişimi yok): `kubectl apply --dry-run` gerçek cluster'a karşı, Argo CD'de "Synced/Healthy" durumu, ilk `git push` sonrası CI'ın doğru image'ı build edip commit attığı, NodePort'ların gerçekten dışarıdan erişilebilir olduğu.

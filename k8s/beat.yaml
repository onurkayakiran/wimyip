apiVersion: apps/v1
kind: Deployment
metadata:
  name: beat
  namespace: wimyip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beat
  template:
    metadata:
      labels:
        app: beat
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: beat
          image: ghcr.io/onurkayakiran/wimyip-backend:d282887a92cb5e45b55e72a1b42952a3595d84a5
          command: ["celery", "-A", "app.tasks.celery_app", "beat", "--loglevel=info"]
          envFrom:
            - configMapRef:
                name: wimyip-config
            - secretRef:
                name: wimyip-secrets
